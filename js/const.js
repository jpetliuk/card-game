// Buttons
const buttonAction = document.querySelector(".buttonAction");
const buttonAction2 = document.querySelector(".buttonAction2");

const roomTitle = document.getElementById("roomTitle");
const roomText = document.getElementById("roomText");
const healthBar = document.getElementById("health");
const coinsDisplayer = document.getElementById("coins");
const fuelDisplayer = document.getElementById("fuel");
const roomAndFloor = document.getElementById("roomAndFloor");
const weaponDisplayer = document.getElementById("weapons");
const enemyHealthDisplayer = document.getElementById("enemyHealth");

// Consts
const initialHealth = 100;
const initialCoins = 0;
const initialFuel = 1;
const initialWeaponDamage = 10;

export {
  buttonAction,
  buttonAction2,
  roomTitle,
  roomText,
  healthBar,
  coinsDisplayer,
  fuelDisplayer,
  initialHealth,
  initialCoins,
  initialFuel,
  initialWeaponDamage,
  roomAndFloor,
  weaponDisplayer,
  enemyHealthDisplayer,
};
